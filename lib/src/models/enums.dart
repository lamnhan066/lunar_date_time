enum EventMode { normal, readonly }

enum EventPriority { high, medium, low }

enum RepeatFrequency {
  no,
  // hourly,
  daily,
  weekly,
  monthly,
  yearly,
}

enum ReminderUnit {
  minutes,
  hours,
  days,
  months,
}
